<%= form_with(model: tweet, local: true) do |form| %>
  <% if tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>

      <ul>
        <% tweet.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :text %><br>
    <%= form.text_area :text, placeholder: "Tweet", :style => "width:20rem;" %>
  </div>

  <div class="field">
    <%= form.label :user_id%><br>
    <%= form.number_field :user_id, placeholder: "Id", :style => "width:2rem;" %>
  </div>
<br>
  <div class="actions">
    <%= form.submit%>
  </div><br>
<% end %>
